version: "3.8"
services:
  server:
    build: .
    image: trustgraph-demo:latest
    ports:
      - "8080:8080"
    command: ["python3", "app.py"]

  client:
    image: trustgraph-demo:latest
    depends_on:
      - server
    command: ["python3", "client.py"]